import static java.lang.Math.sqrt;

import java.util.Scanner;

import javafx.scene.transform.Scale;

import static java.lang.Math.sqrt;

public class programa_tres {
    public static void main(String[] args) {
        Scanner entrada = new Scanner(System.in);
        system.out.println("Esta aplicacion calcula el area de un circulo:  ");
        system.out.println("Por favor ingrese el area del circulo, R =  ");
        String R = entrada.nextLine();
        R = R.Double.parseDouble;
        double resultado = PI * pow(R);
}
}
