import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner Entrada = new Scanner (System.in);
        System.out.println("        1-NUEVO ACTOR   2-BUSCAR ACTOR  3-ELIMINAR ACTOR    4-MODIFICAR ACTOR   5-VER TODOS LOS ACTORES     6- VER PELICULAS DE LOS ACTORES  7-VER CATEGORIA DE LAS PELICULAS DE LOS ACTORES         8-SALIR");
        int a = Entrada.nextInt();
while (b = !false)
{
// declaración de switch
switch(expresión)
{
   // declaración case
   // los valores deben ser del mismo tipo de la expresión
   case b == 1:
      // Declaraciones
      break; // break es opcional
   
   case b == 2 :
      // Declaraciones
       // break es opcional
       break;
   // Podemos tener cualquier número de declaraciones de casos o case
   // debajo se encuentra la declaración predeterminada, que se usa cuando ninguno de los casos es verdadero.
   // No se necesita descanso en el case default
   case b == 3 :
   break;
   case b == 4 :
   break;
   case b == 5 :
   break;
   case b == 6 :
   break;
   case b == 7 :
   break;
   case b == 8 :
   b = true;
   default : 
      // Declaraciones
}
}
}
}